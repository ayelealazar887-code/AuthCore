services:
  mysql:
    image: mysql:8.4
    container_name: auth_go
    restart: unless-stopped

    environment:
      MYSQL_ROOT_PASSWORD: rootadmin
      MYSQL_DATABASE: authGO

    ports:
      - "3306:3306"

    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data: